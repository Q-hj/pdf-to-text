version: '3.8'

services:
  pdf-to-text:
    build: .
    container_name: pdf-to-text
    ports:
      - "3000:3000"
    volumes:
      - ./temp:/app/temp
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped